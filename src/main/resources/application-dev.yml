# Development profile: verbose logging, Docker MySQL
spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:49154/hellapi?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: mysqlpw
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
